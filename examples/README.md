## Example GMPL models

Example models, ranging from very basic demonstrations to full Osemosys.

The smallest will run in under a millisecond, the biggest up to 40 seconds.

Each one includes a model file `.mod`, a data file (optional) `.dat` and an output matrix `.mps`.
